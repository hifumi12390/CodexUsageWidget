# 検証結果 — 2026-09-08

## v1.1（現行）

最終配布物: `dist/v1.1.0-win-x64/CodexUsageWidget.exe`、`dist/CodexUsageWidget-1.1.0-win-x64.zip`。
40項目PASS（`tests/release-1.1/results.txt`）。既存テストに、ダーク時のバー見出しの配色、旧設定からの移行、ライト／システム同期、Windowsテーマ読取、5h/Week個別切替、残量の補数表示、ネットワーク再取得なしの切替、テーマ・表示モードの再起動復元を追加。ライト・ダーク・画像背景のWPF描画PNGを確認した。

旧版の実画面で背景サブメニューの白背景・白文字を確認。ContextMenuだけの着色を廃止し、子メニューのPopupを含む明示的なテーマ対応テンプレートに変更。画像選択は直接メニューから開ける構成にした。

Computer UseはユーザーのEsc操作で停止された。以後の実画面操作は中止。修正後のOSファイルダイアログからの選択・物理クリック・OSテーマ変更へのリアルタイム追従のE2Eは未確認。画像取込・保存復元とテーマ判定は自動テスト済み。

起動済みの途中版に触れず完成版を版別フォルダーへ配置したため、利用時は既存ウィジェットを終了して現行EXEから起動する。

## v1.0（履歴）

完成した.NET 10.0.11同梱win-x64 EXEを使用。ビルド成功、UI/ロジック統合テスト30件PASS。詳細出力は`tests/release/results.txt`、描画結果は同フォルダー内PNG。

確認済み:

- ネイティブWPFウィンドウの起動とHWND生成
- 初回デフォルト設定
- 5h / Weekの使用割合とバー、Credits残高の表示
- 期間による枠判定、複数枠マップの優先、null・未提供の扱い
- コンテキストメニューの実際のオープン
- 全5切替項目のメニューイベントと設定・表示・Topmostへの反映
- 手動更新メニューから取得処理が起動
- 取得失敗で前回値保持・エラー表示、再成功で回復
- 最小サイズのスクロール表示と大きいサイズの描画
- 実ウィンドウ位置・サイズの変更と終了後の復元
- 設定の原子的書込、バックアップ、破損復旧、保存失敗の非クラッシュ化
- 背景画像取込、元ファイル削除後の表示、終了後の画像設定復元
- 配布EXE自身によるライブ取得で5h / Week / Reset / Creditsすべて成功
- 取得終了後に診断用ウィジェットプロセスが残っていないこと
- ソースにRunキー・Startup・タスクスケジューラ登録処理がなく、自動起動を追加していないこと

画像のビジュアル検査はWPF RenderTargetBitmapによる。テストデータの数値は検証専用で、通常起動には使用しない。

確認方法の限界:

- メニューの操作はWPFイベント経由、位置・サイズは実ウィンドウのプロパティ変更で検証。物理マウスでの右クリック・ドラッグ、Windowsのファイル選択ダイアログ内の操作は自動操作していない。
- 画像取込処理は実ファイルで検証した。ユーザーが持つ個別画像のコーデック互換性は未確認。
- Windows 10別実機、複数モニターの異なるDPI、OS透明効果ON/OFFの組合せは受入確認対象。OSによるアクリル合成はWPF描画PNGには含まれない。
- 自動更新タイマーは実装・接続確認済み。長時間常駐、スリープ/復帰を含む耐久試験は未実施。

再実行: `./Build.ps1 -Test`。ライブ接続のみ確認: `dist/win-x64/CodexUsageWidget.exe --probe tests/live-usage.json`。

## 2026-09-08 v1.2

- ユーザー確認: v1.1 EXEが正常起動。友人へ渡したZIPも動作したとの報告。全UIの確認済みとは扱わない。
- `Build.ps1 -Test` による配布EXEで61項目PASS。結果: tests/release-1.2/results.txt。
- トレイ: Windows Shellへの実アイコン登録・削除が成功。ShowInTaskbar=false、非表示・復元後も維持を確認。
- 通知: 5%ちょうどは対象外、5%未満、両枠のまとめ通知、反復抑制、Reset変更・回復後の再通知、期限切れ・欠損抑制、ON/OFF、通知済み状態の保存を検証。通知送信はテスト用sinkへ差替え、Windowsバナーは自動試験で表示していない。
- ガラス: 初期描画後のDWM acrylic要求をOSが受理し、属性値3を読み戻せた。OFFは属性値1と不透明背景。画像モード、画像への復帰、設定復元も確認。
- 既存機能: バー切替、テーマ、各表示項目、メニュー、手動更新、リサイズ・移動、背景画像取込、取得失敗、設定破損からの回復もPASS。
- 自動起動: コード監査でRunキー、Startup、タスク登録を行う処理なし。追加していない。
- Computer Useは一切使用していない。物理マウスによるトレイ操作、ファイルダイアログ、OS通知バナー、背景の実際のぼかし、Explorer再起動後の復元は目視未確認。既存のユーザーウィジェットには操作を行っていない。
- Windowsの透明効果・通知設定に従う。ガラス非対応環境は単色。通知のOS受理は画面表示を保証しない。
- 配布: dist/v1.2.0-win-x64/CodexUsageWidget.exe と dist/CodexUsageWidget-1.2.0-win-x64.zip。
